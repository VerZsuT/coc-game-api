enum WarFrequency {
  Unknown = 'unknown',
  Always = 'always',
  MoreThanOncePerWeek = 'moreThanOncePerWeek',
  OncePerWeek = 'oncePerWeek',
  LessThanOncePerWheek = 'lessThanOncePerWeek',
  Never = 'never'
}

export default WarFrequency
